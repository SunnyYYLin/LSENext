name: Pre-release

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  prerelease:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc,aarch64-pc-windows-msvc
      - name: Test
        run: cargo test --workspace
      - name: Package x64
        shell: pwsh
        run: |
          dotnet tool restore
          ./scripts/package.ps1 -Architecture x64 -ReleaseTag v0.0.2
      - name: Package arm64
        shell: pwsh
        run: |
          dotnet tool restore
          ./scripts/package.ps1 -Architecture arm64 -ReleaseTag v0.0.2
      - name: Create GitHub pre-release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v0.0.2
          name: LSENext v0.0.2
          prerelease: true
          files: |
            artifacts/*.msi
            artifacts/*.zip
